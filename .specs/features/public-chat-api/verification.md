# Public Chat API verification

**Verdict**: FAIL
**Profile**: standard
**Diff range**: 8fb966f..a78a0f0 (feature commits `98dacb3`, `a04a7c9`, `0900e14`, `a78a0f0`; `1eaf414` and `a41c339` belong to `openapi-export` and are out of scope)
**Round**: 1 - full
**Verifier**: independent sub-agent (author != verifier)

Every proof ran at `a78a0f0` in the real tree (clean, `git status --porcelain` empty before and after). The 50 named Vitest proofs ran in one invocation from `apps/server` (`pnpm exec vitest run <7 files> --reporter=verbose -t "<50-name alternation>"`): `Test Files 7 passed (7)`, `Tests 51 passed | 72 skipped (123)`. Each named test is listed individually as `✓` in the output (the extra match is `inbound.spec.ts > receiveInbound > refuses an invalid phone`, which shares a name with C19). No name matched zero tests. The C41 and C43 shell proofs ran from the repo root.

## Binding sources

| Source | Opened | Contradiction | Uncovered |
| --- | --- | --- | --- |
| ADR-014 `docs/decisions/ADR-014-public-channel-identity-consent.md` | yes - read in full | none (see note 1) | - |
| ADR-013 `docs/decisions/ADR-013-conversation-model.md` | yes - read in full | none | - |
| AD-018 row `.specs/STATE.md:24` | yes | none | - |

Notes:
1. ADR-014 says the visitor "só vê as mensagens da conversa criada na própria sessão" and that the cookie is "escopado a `/api/public/chat`". AD-018 (the user's decision of 2026-09-25, recorded as active in `STATE.md:24`) narrows this: a cut by `seq` (two sessions on the same phone see each other's *new* messages) and `Path=/api/public/chat/<key>`. C31 (`public-chat.spec.ts:725`, `[4, 5]`) asserts the AD-018 behaviour. Both sources are binding and the later, explicit AD decides, so this is not counted as a check contradiction. ADR-014's text is now stale on both points and should be revised or annotated to point at AD-018 (lesson L-052 pattern).
2. ADR-014 test obligations for F3 map to checks: org A's link never writes in B (C16), the visitor does not read the same phone's earlier conversation (C31), no consent means 4xx (C17), the rate limit fires (C34-C37). Maximum message size is C24. Turnstile is C20-C22. The visitor Socket.IO namespace and the Open Graph HTML are out of scope in `plan.md` (they belong to `visitor-realtime` and `web-chat-ui`).
3. ADR-013: dedupe by `externalId` (C26, C45), reopening the same conversation (C28), and `seq` order (C25, C31). The rule of one open conversation per contact and channel is reused unchanged (Swept "concurrency", verified below).

## Checks

All citations are at `a78a0f0`. `pc` = `apps/server/src/modules/channels/public-chat.spec.ts`, `vt` = `apps/server/src/modules/channels/visitor-token.spec.ts`, `db` = `apps/server/src/infrastructure/database.spec.ts`, `sc` = `apps/server/test/schema.spec.ts`, `ib` = `apps/server/src/modules/conversations/inbound.spec.ts`, `ar` = `apps/server/test/architecture.spec.ts`.

| Check | Claim | Proof run | Evidence | Result |
| --- | --- | --- | --- | --- |
| C1 | `withPublicChatKey` reads only the key's org, `[]` for an unknown key, contact read fails | batch run, `✓ reads only the organization of the public chat key` | `db:161` - `expect(byKey).toEqual([{ id: tenantA.organizationId }])`; `db:162` - `expect(unknown).toEqual([])`; `db:163-165` - `withPublicChatKey(..., tx => tx.contact.findMany())` `.rejects.toThrow()` | PASS |
| C2 | `app.public_chat_key` does not outlive the transaction | batch run, `✓ does not leak the public chat key to the next transaction` | `db:184-187` - `await expect(deps.db.withoutTenant((tx) => tx.organization.findMany())).rejects.toThrow()` (x10, after 5 `withPublicChatKey`). A leaked key would take the policy's ELSE branch and return a row, so the rejection is discriminating | PASS |
| C3 | the policy still hides other orgs: `withTenant(A)` reads only A **and** `withUser(u)` reads only u's orgs | batch run, `✓ lists only the caller organizations`, `✓ protects Organization with tenant_isolation` | `withUser` half: `db:200-201` - `expect(listed.map(o => o.id)).toContain(tenantA.organizationId)` / `.not.toContain(tenantB.organizationId)`; `sc:112` - `expect(rows).toEqual([{ protected: true }])` (only that a policy exists). **`withTenant(A)` half: no evidence.** `rg -n "organization\.(findMany\|count)" src test` finds no `withTenant` org list assertion. `db:61-76` reads orgs under `withTenant(A)` but asserts only the member ids, and those stay green even if org B leaked | FAIL |
| C4 | `ConsentRecord` RLS ENABLE+FORCE+`tenant_isolation`, composite FKs Restrict, generic checks green | batch run, 3 `✓` | `sc:1036` - `expect(forced).toEqual([{ table: 'ConsentRecord', forced: true }])`; `sc:1037-1039` - one policy whose `qual`/`check` contain `app.tenant_id`; `sc:1044-1050` - 4 FKs, each of contact/conversation/channel matches `FOREIGN KEY ("<x>Id", "organizationId") REFERENCES "<X>"(id, "organizationId") ON UPDATE RESTRICT ON DELETE RESTRICT`; `sc:199` - `expect(unprotected).toEqual([])`; `sc:436` - `expect(findSimpleTenantRelations(schema)).toEqual([])` | PASS |
| C5 | token round-trips, shape `v1.<b64url>.<b64url>` | batch run, `✓ round-trips the visitor session` | `vt:33` - `expect(token).toMatch(/^v1\.[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+$/)`; `vt:34` - `expect(readVisitorToken(key, token, issuedAt)).toEqual(session)` | PASS |
| C6 | `null` for payload byte, signature byte, other secret, `v2.`, exp 1 s past, garbage; accepts 1 s before exp | batch run, 2 `✓` | `vt:41-54` - cases `payload byte`, `signature byte`, `other secret`, `version` (`v2.`), `expired` (`expiresAt + 1000`), `garbage` (+ `empty`, `extra part`) each `expect(readVisitorToken(key, value, now), name).toBeNull()`; `vt:60` - `expect(readVisitorToken(key, token, new Date(expiresAt - 1000))).toEqual(session)` | PASS |
| C7 | `onReceived` called once with tx and `{conversationId, messageId, created: true}`; not for a duplicate | batch run, `✓ runs the received step ...` | `ib:452` - `expect(repeated.created).toBe(false)`; `ib:453-464` - `expect(calls).toEqual([{ result: { conversationId, messageId, created: true, contactId, seq: 1 }, seen: 1 }])` (`seen: 1` = the uncommitted message is visible through the passed `tx`) | PASS |
| C8 | `onReceived` throwing rolls back message, contact, conversation, `lastSeq` | batch run, `✓ rolls the message back ...` | `ib:481-490` - both calls `.rejects.toThrow('consent failed')`; `ib:493` - `expect(await rowsOf(tenant)).toEqual(before)`; `ib:494` - `expect(... .lastSeq).toBe(1)` | PASS |
| C9 | `GET /:key` 200 exact 6 keys, seeded values, `hasLogo` after logo, `noticeVersion`, `turnstileSiteKey` | batch run, `✓ describes the web chat of the key` | `pc:184` - `expect(before.statusCode).toBe(200)`; `pc:185-192` - `toEqual({ name: 'Corretora Alfa', brandColor: '#0055aa', greeting: 'Bem-vindo à Alfa!', hasLogo: false, noticeVersion: '2026-09-25', turnstileSiteKey: null })`; `pc:194` - `toEqual({ ...before.json(), hasLogo: true })`; `pc:199-206` - B's values with `turnstileSiteKey: 'site-key-public'` | PASS |
| C10 | unknown well-formed key -> `404 NOT_FOUND` on all 5 routes | batch run, `✓ answers not found for an unknown key` | `pc:218-228` builds the 5 requests (cookie of another key on the two cookie routes); `pc:231` - `expect(response.statusCode, response.body).toBe(404)`; `pc:232` - `expect(response.json().error.code).toBe('NOT_FOUND')` | PASS |
| C11 | start -> 201, INBOUND/CONTACT/TEXT on WEB_CHAT, contact E.164 | batch run, `✓ starts a session with the first message` | `pc:274` - `toBe(201)`; `pc:279` - `expect(conversation.contact.phoneE164).toBe(normalizePhone(phone))`; `pc:280` - `expect(conversation.channel.kind).toBe('WEB_CHAT')`; `pc:281-288` - `toMatchObject({ direction: 'INBOUND', author: 'CONTACT', kind: 'TEXT', text: body.text, ... })`; `pc:289-299` - exact `{ message }` body | PASS |
| C12 | new conversation `OPEN` / `QUEUE` | batch run, `✓ opens the new conversation in the queue` | `pc:309` - `expect(conversation.status).toBe('OPEN')`; `pc:310` - `expect(conversation.handler).toBe('QUEUE')` | PASS |
| C13 | one `ConsentRecord` per start with contact, conversation, channel, `noticeVersion`; two starts -> two | batch run, `✓ records the consent of every session start` | `pc:325` - `expect(afterFirst).toHaveLength(1)`; `pc:326-331` - `toMatchObject({ contactId, conversationId, channelId, noticeVersion: '2026-09-25' })`; `pc:332` - `expect(afterSecond).toHaveLength(2)` | PASS |
| C14 | cookie attributes; `Secure` only in production via `visitorCookieFor(loadConfig(production))` | batch run, `✓ sets the visitor cookie on the link path`, `✓ marks the cookie secure in production` | `pc:368` - `toMatch(/^bens_visitor=v1\.[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+$/)`; `pc:369-371` - attributes `toEqual(['HttpOnly','Max-Age=2592000','Path=/api/public/chat/<key>','SameSite=Lax'].sort())` (no `Secure`); `vt:70` - `expect(secure).toBe(`${plain}; Secure`)`; `vt:82-85` - `visitorCookieFor(loadConfig({...env, NODE_ENV:'production'}), ...)` `toBe(secure)`, `test` -> `toBe(plain)`; the route uses that function at `public-chat.routes.ts:125` | PASS |
| C15 | one `message.created` with org, conversation, message | batch run, `✓ notifies the panel of the visitor message` | `pc:387-394` - `expect(events).toEqual([{ type: 'message.created', organizationId: target.organizationId, conversationId, messageId }])` | PASS |
| C16 | A's start writes only in A; B's start creates B's rows and never touches A | batch run, `✓ writes only in the organization of the key` | `pc:409` - `expect(await rowsOf(a)).toEqual({ contacts: 1, conversations: 1, messages: 1, consents: 1 })`; `pc:410` - `expect(bAfterA).toEqual(empty)`; `pc:411` - B has 1/1/1/1; `pc:413` - `expect(conversationB.id).not.toBe(conversationA.id)`; `pc:414` - A `lastSeq` `toBe(1)` | PASS |
| C17 | `consent: false` and missing -> 400, nothing stored | batch run, `✓ refuses a start without consent` | `pc:426-427` - both `toBe(400)`; `pc:428` - `expect(await rowsOf(target)).toEqual(empty)` | PASS |
| C18 | outdated notice -> `422 NOTICE_OUTDATED`, nothing stored | batch run, `✓ refuses an outdated notice` | `pc:436-438` - `toBe(422)`, `error.code` `toBe('NOTICE_OUTDATED')`, `rowsOf` `toEqual(empty)` | PASS |
| C19 | phone `123` -> `422 INVALID_PHONE`, nothing stored | batch run, `✓ refuses an invalid phone` (public-chat.spec) | `pc:446-448` - `toBe(422)`, `toBe('INVALID_PHONE')`, `toEqual(empty)` | PASS |
| C20 | siteverify `success:false` -> `403 TURNSTILE_FAILED`, nothing stored | batch run, `✓ refuses a start the captcha rejects` | `pc:517-519` - `toBe(403)`, `toBe('TURNSTILE_FAILED')`, `toEqual(empty)` | PASS |
| C21 | siteverify got exactly one request with `secret` and `response` | batch run, `✓ verifies the captcha token with the secret` | `pc:538` - `toBe(201)`; `pc:539` - `expect(verify.calls).toEqual([{ secret: 'turnstile-secret', response: 'widget-token-123' }])` | PASS |
| C22 | siteverify unreachable -> 403, nothing stored (fail closed) | batch run, `✓ fails closed when the captcha is unreachable` | `pc:556-558` - `toBe(403)`, `toBe('TURNSTILE_FAILED')`, `toEqual(empty)` | PASS |
| C23 | production rejects a missing secret / site key in every `SIGNUP_MODE`, naming the variable | batch run, `✓ requires turnstile keys in production in every signup mode` | `apps/server/src/shared/config.spec.ts:68` - `for (const mode of ['closed', 'self_serve'])`; `:74-77` - `toThrow(ConfigError)` + `toThrow(/TURNSTILE_SECRET_KEY/)` / `toThrow(/TURNSTILE_SITE_KEY/)`; `:78` - loads with both keys. Precision gap: the check text says `invite_only`, which is not a member of `SIGNUP_MODE` (`config.ts:28` - `z.enum(['closed', 'self_serve'])`). The proof covers every real member | PASS |
| C24 | 400 on unknown field, `''`, 4 001 chars, bad uuid on both POSTs; 4 000 accepted; bad key on every route; bad query | batch run, `✓ validates the public chat inputs` | `pc:482-483` - each bad body `toBe(400)` on `/sessions` and `/messages`; `pc:486` - `expect(longest.statusCode).toBe(201)` (4 000 chars, `/messages` only); `pc:500` - `NAO-HEX` key `toBe(400)` on the 5 routes; `pc:503-504` - `?foo=1` and `?after=-1` `toBe(400)` | PASS |
| C25 | `POST /messages` 201 into the token's conversation, next `seq` | batch run, `✓ sends a message within the session` | `pc:577` - `toBe(201)`; `pc:578-581` - `toEqual([[1, 'Olá, quero cotar um seguro.'], [2, 'Tenho um carro 2020']])`; `pc:582` - `toMatchObject({ id: stored[1]?.id, seq: 2, author: 'CONTACT' })` | PASS |
| C26 | repeat -> 201 then 200 with same id, one message | batch run, `✓ answers a repeated message with the original` | `pc:594-596` - `toBe(201)`, `toBe(200)`, `repeated...id` `toBe(first...id)`; `pc:597` - `messages` `toBe(2)` (start + one) | PASS |
| C27 | id of another conversation -> `409 CONFLICT`, no id/text leaked | batch run, `✓ refuses a message id of another conversation` | `pc:610-613` - `toBe(409)`, `toBe('CONFLICT')`, `body` `not.toContain(original id)` and `not.toContain('Segredo do primeiro')` | PASS |
| C28 | CLOSED conversation reopens, same id, cookie still works | batch run, `✓ reopens the closed conversation of the session` | `pc:631` - `toBe(201)`; `pc:633` - `expect(reopened.id).toBe(conversation.id)`; `pc:634` - `toBe('OPEN')`; `pc:635` - `listed.statusCode` `toBe(200)` | PASS |
| C29 | 401 `VISITOR_SESSION_REQUIRED` on both cookie routes for missing, tampered, expired, other-org cookie | batch run, `✓ requires a valid visitor session` | `pc:659-664` - the 4 cases; `pc:676` - `expect(response.statusCode, name).toBe(401)`; `pc:677` - `toBe('VISITOR_SESSION_REQUIRED')` for `sent` and `listed` | PASS |
| C30 | `GET /messages` 200, ascending `seq`, exact 7 keys | batch run, `✓ lists the session messages in seq order` | `pc:696` - `toBe(200)`; `pc:698` - `expect(items.map(i => i.seq)).toEqual([1, 2, 3])`; `pc:700-702` - `Object.keys(item).sort()` `toEqual([...7 keys].sort())`. **The ascending-order half does not discriminate:** the rows are inserted in `seq` order, and removing `orderBy: { seq: 'asc' }` (`public-chat.ts:245`) leaves this proof and C28/C31/C32/C33 green (fault 5) | FAIL |
| C31 | earlier seq 1-3 hidden; own seq 4; later seq 5 shown | batch run, `✓ hides every message before the session start` | `pc:723` - `expect(response.json().message.seq).toBe(4)`; `pc:724` - `expect(seqs(afterStart)).toEqual([4])`; `pc:725` - `toEqual([4, 5])`; `pc:726` - `later.body` `not.toContain('Antes')` | PASS |
| C32 | `after` filters by `seq > after` and `≥ fromSeq`; 100 per page; past end `[]` | batch run, `✓ reads the session after a seq, a hundred at a time` | `pc:745` - `?after=1` (below `fromSeq` 3) `toEqual([3, 4, 5, 6, 7])`; `pc:746` - `?after=4` `toEqual([5, 6, 7])`; `pc:747` - 100 items `3..102` of 103; `pc:748` - `?after=105` `toEqual({ items: [] })`. The check text says `after=2` and "105 messages"; the test uses equivalent values (`after=1`/`after=4`, 103 in the session, 105 in the conversation) | PASS |
| C33 | HUMAN reply visible, OUTBOUND, no `authorUserId` | batch run, `✓ shows a human reply to the visitor` | `pc:760` - `toHaveLength(1)`; `pc:762-767` - `toMatchObject({ seq: 2, author: 'HUMAN', direction: 'OUTBOUND', text })`; `pc:768` - `expect(reply).not.toHaveProperty('authorUserId')` | PASS |
| C34 | 6th start from one IP -> 429, nothing stored for it | batch run, `✓ limits session starts per ip` | `pc:792` - `toEqual([201, 201, 201, 201, 201])`; `pc:793-794` - `toBe(429)`, `toBe('RATE_LIMITED')`; `pc:795` - `messages` `toBe(5)` | PASS |
| C35 | 61st start per key from new IPs -> 429; other key still 201 | batch run, `✓ limits session starts per key` | `pc:809` - `statuses.every(s => s === 201)` `toBe(true)`; `pc:810-811` - `toBe(429)`, `toBe('RATE_LIMITED')`; `pc:812` - `otherKey` `toBe(201)` | PASS |
| C36 | 21st `POST /messages` per token -> 429; other token, same IP, 201 | batch run, `✓ limits messages per visitor` | `pc:828-831` - 20x 201, `toBe(429)`, `toBe('RATE_LIMITED')`, `otherToken` `toBe(201)` | PASS |
| C37 | 121st public GET from one IP -> 429 | batch run, `✓ limits public reads per ip` | `pc:844-846` - 120x 200, `next.statusCode` `toBe(429)`, `toBe('RATE_LIMITED')`. Only `GET /api/public/chat/:key` is called (`pc:840-842`); see Coverage | PASS |
| C38 | after exhausting public limits, `/api/health` and `/api/v1/me` are non-429 | batch run, `✓ limits only the public chat routes` | `pc:862` - `publicRead` `toBe(429)` (limit really exhausted); `pc:863` - `health` `toBe(200)`; `pc:864` - `me` `toBe(200)` | PASS |
| C39 | logo bytes and `Content-Type`; the other key serves its own; no logo -> 404 | batch run, `✓ serves the logo of the key` | `pc:257-258` - `toBe(404)`, `toBe('NOT_FOUND')`; `pc:259-261` - `toBe(200)`, `content-type` `toBe('image/png')`, `rawPayload.equals(PNG)` `toBe(true)`; `pc:262-263` - B `image/webp` and B's bytes | PASS |
| C40 | foreign `Origin` start -> `403 ORIGIN_NOT_ALLOWED`, nothing stored | batch run, `✓ refuses a public write from another origin` | `pc:457-459` - `toBe(403)`, `toBe('ORIGIN_NOT_ALLOWED')`, `toEqual(empty)` | PASS |
| C41 | 5 `operationId`s; `api:generate` leaves no diff | `pnpm api:generate` exit 0; `git add --intent-to-add ... && git diff --exit-code -- apps/server/openapi.json apps/web/src/api` exit 0; `grep -c ... openapi.json` printed `5` | `apps/server/src/modules/channels/public-chat.routes.ts:83` `'getPublicChat'`, `:97` `'getPublicChatLogo'`, `:118` `'startPublicChatSession'`, `:138` `'sendPublicChatMessage'`, `:161` `'listPublicChatMessages'`; real-tree porcelain empty after the run | PASS |
| C42 | `ConsentRecord` owned by `contacts`; writes via a `contacts` export; no cycle; no `id` in inputs | batch run, 4 `✓` in `test/architecture.spec.ts` | `ar:144` - `ConsentRecord: 'contacts'`; `ar:392` - `expect(findForeignWrites(readSourceTree(srcRoot))).toEqual([])`; `ar:374` - `expect(findModuleCycles(...)).toEqual([])`; `ar:280` - `expect(findBoundaryViolations(...)).toEqual([])`; `ar:265` - `expect(offenders).toEqual([])`. The write goes through `recordConsent` (`contacts/consent.ts:13`) from `channels/public-chat.ts:145` | PASS |
| C43 | `@fastify/rate-limit` 11.x, registered `global: false` | grep printed `1`; batch run `✓ limits only the public chat routes` | `apps/server/package.json:19` - `"@fastify/rate-limit": "11.2.0"`; `apps/server/src/app.ts:71` - `app.register(fastifyRateLimit, { global: false })`; `pc:863-864` - non-public routes `toBe(200)` | PASS |
| C44 | `WEB_CHAT_NOTICE_VERSION` is `'2026-09-25'` and reported | batch run, `✓ describes the web chat of the key` | `pc:193` - `expect(WEB_CHAT_NOTICE_VERSION).toBe('2026-09-25')`; `pc:190` - `noticeVersion: '2026-09-25'` in the exact body | PASS |
| C45 | repeated start -> 201 same message; same id + other phone -> 409 without id/text; 1/1/1/1 rows | batch run, `✓ answers a repeated start with the same session` | `pc:347-349` - `toBe(201)` twice, `repeated.json()` `toEqual(first.json())`; `pc:350` - `toBe(409)`; `pc:351-352` - `not.toContain(id)`, `not.toContain(body.text)`; `pc:353-358` - `rowsOf` `toEqual({ contacts: 1, conversations: 1, messages: 1, consents: 1 })` | PASS |

43/45 PASS, 2 FAIL (C3, C30).

## Coverage

Recomputed from `plan.md` `Surface` (the contract the routes must satisfy) and from `public-chat.routes.ts` / `public-chat.ts` (the statuses the code can actually emit), not from the checks' own table.

| Set (size) | Recomputed from | Member -> proof | Unproven |
| --- | --- | --- | --- |
| `GET /:key` statuses (4) | Surface; `public-chat.routes.ts:76-88`, `public-chat.ts:22,74` | 200 C9 (`pc:184`) · 400 C24 (`pc:500`) · 404 C10 (`pc:231`) · 429 C37 (`pc:845`) | - |
| `GET /:key/logo` statuses (4 in Surface + 304 emitted) | Surface; `public-chat.routes.ts:91-108` | 200 C39 (`pc:259`) · 400 C24 (`pc:500`) · 404 C10 (`pc:231`), C39 (`pc:257`) · 429 **none**: C37 calls only `GET /:key` (`pc:840-842`) · 304 **none**: `routes.ts:105` answers 304 on `If-None-Match`, which Surface does not list and no public-route test sends (`branding.spec.ts:339` covers the panel route) | 429 on `/logo`; 304 on `/logo` |
| `GET /:key/logo` output (3) | Surface Out: bytes, `Content-Type`, `Cache-Control` (+ `ETag`, Flow 7) | bytes C39 (`pc:261`) · `Content-Type` C39 (`pc:260`) · `Cache-Control` **none** (`routes.ts:104` sets `public, no-cache`; `rg -n "cache-control" pc` finds nothing) · `ETag` **none** | `Cache-Control`; `ETag` |
| `POST /sessions` statuses (7) | Surface; `routes.ts:110-128`, `public-chat.ts:118-176` | 201 C11, C45 · 400 C17, C24 · 403 C20, C22, C40 · 404 C10 · 409 C45 (`pc:350`) · 422 C18, C19 · 429 C34, C35 | - |
| `POST /messages` statuses (8) | Surface; `routes.ts:130-151`, `public-chat.ts:188-229` | 200 C26 · 201 C25 · 400 C24 · 401 C29 · 403: the app-level `onRequest` at `app.ts:131-137` runs before the route hooks for every mutating path, and `app.spec.ts:218-232` asserts it on arbitrary paths. Accepted · 404 C10 · 409 C27 · 429 C36 | - |
| `GET /messages` statuses (5) | Surface; `routes.ts:153-172` | 200 C30 · 400 C24 (`pc:500,503-504`) · 401 C29 · 404 C10 · 429 **none**: C37 and C38 call only `GET /:key` | 429 on `GET /messages` |
| Landing doors (7) | plan `Landing` | 1 C1, C2 (C3 partly, see Checks) · 2 C4, C13 · 3 C7, C8 · 4 C5, C6, C14 · 5 C31, C32 (C30's order half is unproven, fault 5) · 6 C34-C38, C43 · 7 C26, C27, C45 | door 5 "`seq` crescente" (order) |
| Invalid-cookie causes (4) | AC 21 | missing, tampered, expired, other org: C29 (`pc:659-664`, `676-677`) | - |
| Token rejections at its own layer (6) | C6 claim | payload, signature, secret, version, expired, garbage: `vt:41-54` | - |
| Start refusals (5) | AC 10-13 + dependency failure | C17, C18, C19, C20, C22 | - |
| Invalid inputs (AC 16 + query) | AC 16, Surface In | unknown field, `''`, 4 001, bad uuid (both POSTs) `pc:482-483` · bad key on 5 routes `pc:500` · `?foo`, `after=-1` `pc:503-504` · missing `consent` `pc:427` | - |
| Rate limits (door 6: 4 limits over 5 routes) | Landing 6; `routes.ts:69-74,85,99,120,140,163` | start/IP C34 · start/key C35 · messages/token C36 · reads/IP on `GET /:key` C37 · on `GET /logo` none · on `GET /messages` none · only public routes C38 | reads/IP on `/logo` and `GET /messages` (same as the status rows) |
| Cookie attributes (6) | door 4 / AD-018 | `HttpOnly`, `SameSite=Lax`, `Path`, `Max-Age` `pc:369-371` · `Secure` `vt:70,82` · shape `pc:368`, `vt:33` | - |
| `PublicMessage` keys (7) | Surface | exact key set `pc:700-702`; `authorUserId` absent `pc:768` | - |
| Description keys (6) | Surface / AC 1 | exact object `pc:185-192`, `199-206` | - |
| Turnstile in production (`SIGNUP_MODE` members: 2) | `config.ts:28` enum | `closed` and `self_serve`, secret and site key each: `config.spec.ts:68-78` | - (the checks' `invite_only` label does not exist; precision gap) |
| Startup assemblies registering the limiter (2) | `rg -n "buildApp\(" src test`: `src/server.ts:22`, `test/app.ts:42` | both call `buildApp`, which registers at `app.ts:71`, so the tests' assembly is the production one | - |
| `ConsentRecord` relations (4 FKs) | plan `Relations` | `sc:1044-1050` | - |

## Test policy rows

| Row | Files it classifies | Required proof | Expectation met |
| --- | --- | --- | --- |
| `visitor-token.ts` (sign/verify, expiry, cookie attributes): own layer **and** boundary | `apps/server/src/modules/channels/visitor-token.ts` | own layer: C5 `vt:33-34`, C6 `vt:41-54,60`, C14 `vt:67-85` · boundary: C29 `pc:659-677`, C14 `pc:368-371` | yes. Each C6 rejection cause is asserted at its own layer and each cookie cause at the route. Note: the branch where the signature is valid but the JSON or schema parse fails (`visitor-token.ts:63-70`) has no case. Only the key holder can reach it, and the row's expectation names C6's causes, so it is recorded as an observation, not a miss |

Swept `existing` re-read: "concurrency: two first messages of one phone share one contact and one conversation". The constraint is present: `prisma/migrations/20260924160000_conversation_core/migration.sql:150` (`CREATE UNIQUE INDEX "Conversation_one_open"`), proven by `ib:268` `creates one contact and conversation for a concurrent first contact`. Holds.

"Settled mid-build" claims re-checked: the 409 on `/sessions` is in Surface and proven (C45). C23's mode is a precision gap, as the author says. The C14 production half goes through `visitorCookieFor(config)`, confirmed at `routes.ts:125`. C2's fail-closed rejection is confirmed (`db:184-187`). One `onRequest` per route with `createRateLimit` is confirmed at `routes.ts:39-53`, and both start limiters fire (C34, C35). `onboarding.spec.ts` no longer imports `channels`, confirmed in the `a78a0f0` diff.

## Faults injected

Scratch worktree at `a78a0f0` (`git worktree add`, `pnpm install --frozen-lockfile`). One process at a time; the worktree was reset between faults (`git checkout -- .`) and removed with `git worktree remove --force`. Real-tree `git status --porcelain` matched the empty baseline afterwards.

| Mutation | Location | Killed |
| --- | --- | --- |
| dropped the `fromSeq` cut: `gte: Math.max(session.fromSeq, (after ?? 0) + 1)` -> `gte: (after ?? 0) + 1` | `apps/server/src/modules/channels/public-chat.ts:243` | yes - C31 `expected [ 1, 2, 3, 4 ] to deeply equal [ 4 ]` |
| dropped the token's organization check in `sessionOf`: `if (!session \|\| session.organizationId !== organizationId)` -> `if (!session)` | `public-chat.ts:184` | yes - C29 `other organization: expected 201 to be 401` (the message was written into org A through B's link) |
| removed the consent write from `startSession`'s `onReceived` (the step became a no-op) | `public-chat.ts:144-150` | yes - C13 `expected [] to have a length of 1 but got +0` |
| widened the `Organization` policy's key branch: `OR "publicChatKey" = NULLIF(current_setting('app.public_chat_key', true), '')` -> `OR NULLIF(current_setting('app.public_chat_key', true), '') IS NOT NULL` (any key reveals every organization) | `apps/server/prisma/migrations/20260925112553_public_chat/migration.sql:52` | yes - C1 `expected [ …(2) ] to deeply equal [ Array(1) ]` |
| removed `orderBy: { seq: 'asc' }` from `listVisitorMessages` | `public-chat.ts:245` | no - C28, C30, C31, C32, C33 all passed (`Tests 5 passed`) |

Five faults (the cap). They made C1, C13, C29 and C31 fail. The fifth checked whether C30 can detect an ordering fault, and it survived. Not injected: the rate-limit `max`, the conversation-mismatch `409` (`public-chat.ts:223`), and the Turnstile fail-closed `catch` (`turnstile.ts:38-40`). Reading their assertions (`pc:793`, `pc:610`, `pc:556`) shows each would fail under a direct mutation.

## Gate

`pnpm lint && pnpm typecheck && pnpm test && pnpm build` from the repo root at `a78a0f0`, run after the fault runs: exit 0. Biome `Checked 199 files ... No fixes applied`. Typecheck clean. Vitest `Test Files 42 passed (42)`, `Tests 460 passed (460)`. Server and web builds done. The real tree stayed clean afterwards.

## Ranked gaps

1. Surviving mutant: removing the `seq` sort from `listVisitorMessages` leaves every read proof green. Door 5's "`seq` crescente" and C30's "in ascending `seq`" are unproven. Seed a session whose physical row order differs from `seq`, for example by updating an earlier message so its tuple moves, or read through a page boundary, so that dropping `orderBy` fails. - C30 - `apps/server/src/modules/channels/public-chat.ts:245`, `public-chat.spec.ts:698`
2. C3's `withTenant(A)` half ("reads only A" after the new policy branch) has no located assertion. The named proofs cover `withUser` and the existence of a policy only. - C3 - no evidence (`database.spec.ts:200-201`, `schema.spec.ts:112` cover the other half)
3. The 120/min per-IP read limit is attached to `GET /logo` and `GET /messages` (`public-chat.routes.ts:99,163`), but the only proofs (C37, C38) call `GET /:key`. The checks' Coverage table credits C37 with the `429` of both routes. - C37 - `public-chat.spec.ts:840-846`
4. `GET /logo` emits `ETag`, `Cache-Control` and `304` (`public-chat.routes.ts:104-105`). Surface names `Cache-Control` as an output, and nothing on the public route asserts it. `304` is missing from Surface's status list, and no proof covers it. - C39 - `public-chat.spec.ts:236-264`
5. Precision gaps in the checks' text. None fails the feature on its own:
   - C23 names `SIGNUP_MODE=invite_only`, which does not exist (`config.ts:28`); the same mistake is in plan Impact.
   - C32's literal values (`after=2`, "105 messages") differ from the test (`after=1`/`after=4`, 103 messages in the session).
   - C24's "4 000 accepted" is proven on `POST /messages` only.
   - C31's "message sent by the earlier session" is seeded with the `inbound()` helper, not through the earlier session's cookie.
6. Binding-source hygiene: ADR-014's text ("only the conversation created in its own session", cookie scoped to `/api/public/chat`) is superseded by AD-018 and the user's 2026-09-25 decision, but the ADR was not revised.
